export enum SyncActionTypes {
    ROUTE = 1,
    UTILITY
}

export enum WriteOperationTypes {
    CREATE = 1,
    UPDATE,
    DELETE
}

export enum PamlightDBWriteTypes {
    CREATE_DOCUMENT = 1,
    CREATE_DOCUMENTS,
    UPDATE_DOCUMENT,
    UPDATE_DOCUMENTS,
    DELETE_DOCUMENT,
    DELETE_DOCUMENTS
}
